## What does this change, and why?

<!-- A sentence or two. For anything visual, a screenshot helps a lot. -->

## Checklist

- [ ] `npm test` passes
- [ ] Checked at phone width (about 360–412px) — nothing spills off the right edge
- [ ] Nothing new leaves the user's browser (no analytics, trackers, or calls to APIs/servers)
- [ ] Any new saved data has a default in `migrateState()` so existing users' data still loads
